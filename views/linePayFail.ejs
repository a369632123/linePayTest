<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>linePayFail</title>
</head>
<body>
    <h1>付款失敗</h1>
    <% if (order) { %>
        <p>訂單 ID: <%= order.orderId %></p>
        <p>付款金額: <%= order.amount %><%= order.currency %></p>
    <% } else { %>
        <p>訂單 ID 未提供</p>
    <% } %>
</body>
</html>